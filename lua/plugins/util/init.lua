-- Utils contain plugins which enhance or replace existing functionality. If a plugin was to provide something entirely
-- new or an important feature then it should be added in the `plugins/` directory.
require('plugins.util.edit')
require('plugins.util.file')
require('plugins.util.keymap')
require('plugins.util.navigation')
